# =============================================================================
# Azure OpenAI Proxy — Environment Configuration
# Copy this file to .env and fill in your values.
# =============================================================================

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------

# Your Azure OpenAI resource endpoint (include trailing slash)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# Default API key — used when the client request carries no key of its own.
# Clients may also supply their own key via Authorization: Bearer or api-key header.
AZURE_OPENAI_API_KEY=your-api-key-here

# -----------------------------------------------------------------------------
# API versions
# Each route type uses a separate api-version. Override only what you need.
# -----------------------------------------------------------------------------

# Chat completions, embeddings, audio, images, files, fine-tunes
# Applied as ?api-version= on /openai/deployments/... requests
# Default: 2024-08-01-preview
AZURE_OPENAI_APIVERSION=2024-12-01-preview

# GET /v1/models — lists available deployments from Azure
# Default: 2024-10-21
AZURE_OPENAI_MODELS_APIVERSION=2024-10-21

# Responses API — used for o-series (o1/o3/o4) and direct /v1/responses requests
# Default: 2024-08-01-preview
# AZURE_OPENAI_RESPONSES_APIVERSION=2024-08-01-preview

# Anthropic Messages API — sent as the anthropic-version header (no api-version param)
# Default: 2023-06-01
# ANTHROPIC_APIVERSION=2023-06-01

# -----------------------------------------------------------------------------
# Proxy settings
# -----------------------------------------------------------------------------

# Address and port the proxy listens on
# Default: 0.0.0.0:11437
# AZURE_OPENAI_PROXY_ADDRESS=0.0.0.0:11437

# Proxy mode: "azure" routes to Azure OpenAI; "openai" passes through to OpenAI directly
# Default: azure
# AZURE_OPENAI_PROXY_MODE=azure

# Upstream endpoint used only when AZURE_OPENAI_PROXY_MODE=openai
# Default: https://api.openai.com
# OPENAI_API_ENDPOINT=https://api.openai.com

# -----------------------------------------------------------------------------
# Model mapper overrides
# Comma-separated list of model=deployment pairs.
# These override the 180+ built-in defaults in src/models/mapper.ts.
# Example: pin gpt-5-mini to a specific deployment name on your resource.
# -----------------------------------------------------------------------------

# AZURE_OPENAI_MODEL_MAPPER=gpt-5-mini=gpt-5-mini,my-model=my-deployment

# -----------------------------------------------------------------------------
# Serverless deployments (Azure AI Studio / Models-as-a-Service)
# Format: model=DeploymentName:Region
# Each serverless model also needs its own API key (per-model env var).
# Serverless models are routed to https://{DeploymentName}.{Region}.models.ai.azure.com
# -----------------------------------------------------------------------------

# AZURE_AI_STUDIO_DEPLOYMENTS=mistral-large-2407=Mistral-large2:swedencentral
# AZURE_OPENAI_KEY_MISTRAL-LARGE-2407=your-serverless-key-here

# -----------------------------------------------------------------------------
# Per-model endpoint overrides
# Route specific models to different Azure OpenAI resources.
# Format: model=https://endpoint-url:optional-api-key
# Comma-separated for multiple entries. Key is optional — omit :key to use
# the default API key (from request headers or AZURE_OPENAI_API_KEY).
# -----------------------------------------------------------------------------

# AZURE_OPENAI_ENDPOINT_MAP=kimi-k2.5=https://my-other-resource.services.ai.azure.com/:key123

# -----------------------------------------------------------------------------
# Debug logging
# Set to any non-empty value to log full request and response bodies.
# WARNING: produces verbose output — use only for troubleshooting.
# -----------------------------------------------------------------------------

# DEBUG_LOG=1
